# 부동산 스크리너 설계서 v4.0

> **버전**: v4.1.0
> **작성일**: 2026-07-31
> **이전 문서**: v3.3은 폐기. 미룬 항목은 전부 §8에 옮겨 적었으므로 따로 볼 필요 없다
> **분량 원칙**: 이 문서는 A4 4장을 넘기지 않는다. 넘으면 범위가 잘못된 것이다

---

## 0. 왜 범위를 줄였나

v3.3은 팩터 15개, 게이트 20여 개, 화면 3계층을 한 번에 요구했다. 구현 결과 서울 전체 단지가 섞이고, 값이 없는데 문장이 생성되고, 검증 리포트는 전부 "통과"로 나왔다.

**실패 지점이 많으면 조용히 실패한다.** 그래서 다음 원칙으로 다시 시작한다.

| # | 원칙 |
| :-- | :-- |
| 1 | **발주자가 직접 검증할 수 있는 것만 만든다.** 네이버에서 5분 안에 확인 가능해야 한다 |
| 2 | **축을 하나씩 검증한 뒤 합친다.** 이번에는 정렬만 한다. 종합 점수는 §7 참조 |
| 3 | **하나가 확실히 돌아간 뒤 다음을 붙인다** |
| 4 | **안 만드는 것은 §9에 기록한다.** 조용히 빼지 않는다 |

---

## 1. 이번에 만드는 것

```
대상    서초구(11650) + 강남구(11680)
단위    단지 × 평형
데이터  국토부 매매 실거래 (2018-01 ~ 최신)
지표    ① 하락률  ② 초과하락률  ③ 거래량비
출력    표 하나.  초과하락률 순 정렬
```

**이번에 쓰지 않는 것**: 전월세 데이터, 네이버 호가, 카카오 API, 종합 점수, 카드 UI, 판단 문장, 배지, 텔레그램 알림.
전부 §9에 기록되어 있으며 순서대로 붙인다.

---

## 1.1 이전 작업에서 이어 쓰는 것

**처음부터 다시 만들지 않는다.** 2026-07-31 수정분에서 아래는 그대로 쓴다.

| 자산 | 내용 |
| :-- | :-- |
| 대상 구 필터 + `assert` | `build_complex_master.py`, `build_stats.py`에 이미 적용됨. 유지 |
| 국토부 CSV 로더 | 인코딩·헤더 탐색·콤마 제거 처리 완료 |
| 단지 마스터 구축 | 국토부 실거래에서 단지명·지번·건축년도로 파생 |
| 전용면적 관측 범위 | 실거래에서 산출한 min/max |
| 점검 스크립트 | `verify_phase1a.py` 골격. **출력 항목만 §6.2로 교체** |

**버리는 것**: 4-Block 가중합, 점수 산출(Φ 매핑 포함), 판단 문장, 배지, 카드 화면.
점수는 폐기가 아니라 v4.3으로 미룬 것이다 (§7).

## 2. 데이터

### 2.1 입력
`data/raw/molit/<날짜>/` 의 국토부 매매 CSV (서울 전체, 2018-01 ~ 최신)

### 2.2 적재 필터 — 위반 시 즉시 중단

```python
assert sgg_cd in ("11650", "11680"), f"대상 구가 아님: {sgg_cd}"
```

**문서에 적는 것으로 부족하다. 코드에서 막는다.** 서울 전체 원본을 보관하되 DB에는 2개 구만 들어간다.

### 2.3 평형 구분

전용면적(㎡) 기준. 범위 밖은 `None` → 제외.

| 코드 | 전용면적 | 화면 표기 |
| :-- | :-- | :-- |
| `A59` | 50 ≤ x < 70 | 25평형 |
| `A84` | 70 ≤ x < 100 | 34평형 |
| `A114` | 100 ≤ x < 135 | 44평형 |

그 외 면적은 이번 범위에서 제외한다 (§9 항목 1).

### 2.4 유효 거래

```sql
WHERE is_cancelled = 0          -- 해제사유발생일이 있으면 제외
  AND deal_type != '직거래'
  AND area_type IS NOT NULL
```

---

## 3. 계산

단지 × 평형마다 아래 4단계를 계산한다. 기준일은 실행일.

### 3.1 최근 거래가
```
최근거래가 = 최근 3개월 유효거래의 중위값
```
3개월 내 거래가 없으면 → 최근 6개월, 그래도 없으면 → 최근 12개월.
12개월에도 없으면 제외(§4).

### 3.2 전고점
```
기준일부터 60개월 전까지, 3개월 단위로 창을 밀며:
    창 안의 유효거래가 3건 이상이면 → 그 창의 상위 90% 지점 값을 기록
전고점 = 기록된 값들 중 최대값
전고점시점 = 그 창의 마지막 달
```
> 단일 최대값을 쓰지 않는 이유: 이상거래 1건이 전고점이 되는 것을 막는다.
> **시간 감쇠는 적용하지 않는다.** 대신 `전고점시점`을 화면에 표시해 발주자가 직접 판단한다.

#### 왜 3건인가 (2026-07-31 조정)
이전 기준(창당 2건, 12개월 5건 미만 제외 → 3건 미만 제외)으로 산출했을 때 상위권을 소규모 나홀로 단지가 독식했다. 거래가 3~4건뿐이면 중위값과 전고점이 한두 건으로 결정되어 **하락률이 극단적으로 튀고**, 그 극단값이 전체 분포를 끌어당긴다.

기준을 올리면 일부 단지가 표에서 빠지지만, **표본이 얇은 단지의 숫자는 애초에 신뢰할 수 없다.** 빠진 단지는 §4의 제외 목록에서 확인할 수 있다.

### 3.3 하락률 / 초과하락률
```
하락률     = 1 − 최근거래가 / 전고점

강남권중위 = 같은 평형 전체 단지의 하락률 중위값
초과하락률 = 하락률 − 강남권중위
```
`초과하락률`이 양수면 시장 평균보다 더 떨어진 것이다. **시장 전체가 빠진 부분은 신호가 아니므로 빼낸다.**

### 3.4 거래량비
```
거래량비 = 최근 3개월 거래건수 / (최근 12개월 거래건수 ÷ 4)
```
1.0보다 크면 거래가 평소보다 활발하다.

---

## 4. 제외 규칙

| 조건 | 화면 표기 |
| :-- | :-- |
| 최근 12개월 유효거래 **5건 미만** | 최근 1년간 거래가 거의 없습니다 |
| 직거래·해제 비중 30% 초과 | 직거래·취소 거래가 많아 가격을 믿기 어렵습니다 |
| 전고점을 못 구함 | 과거 거래가 부족해 비교 기준을 잡지 못했습니다 |
| 평형 구분 실패 | (표에서 제외. 사유 표기 없음) |

**제외된 단지는 별도 목록으로 볼 수 있게 한다.** 조용히 사라지면 안 된다.

---

## 5. 화면

### 5.1 형태
**표(리스트) 하나.** 카드·배지·이모지·판단 문장·진행 막대를 쓰지 않는다.

### 5.2 컬럼

| 지역 | 단지명 | 평형 | 최근 거래가 | 전고점 | 전고점 시점 | 하락률 | 초과하락 | 거래 건수 | 거래량비 |
|---|---|---|---|---|---|---|---|---|---|
| 서초구 반포동 | 반포자이 | 34평형 | 38.5억 | 44.2억 | 2024.09 | -12.9% | +5.3%p | 11건 | 1.4배 |
| 강남구 대치동 | 은마 | 34평형 | 26.8억 | 29.4억 | 2024.03 | -8.8% | +1.2%p | 23건 | 0.9배 |

- 기본 정렬: **초과하락률 내림차순**
- 모든 컬럼 정렬 가능
- 필터: 지역(서초/강남), 평형(25/34/44)
- 단지명 검색

### 5.3 표기 규칙

| 규칙 |
| :-- |
| 값이 없으면 **빈칸**. `-`, `N/A`, `0`, 추정치 금지 |
| `A84`, `EXCLUDED`, `L1`, `L2`, `PASS`, `z-score` 등 내부 용어를 화면에 쓰지 않는다 |
| 금액은 억 단위 소수 1자리 |
| 하락률·초과하락률은 부호를 값에서만 결정한다. `+-` 같은 표기가 나오면 버그다 |

### 5.4 하단 고정 표시
```
서초구·강남구 아파트 실거래 기준 · 2026년 7월 31일 갱신
대상 단지 612곳 중 431곳 표시 · 181곳 제외 [제외 목록 보기]
```

---

## 6. 검증

### 6.1 발주자 검증 (최종 판정)
표에서 **아무 행이나 3개**를 골라 네이버 부동산에서 확인한다.

| 확인 항목 | 판정 |
| :-- | :-- |
| 그 단지가 서초구 또는 강남구에 있는가 | 아니면 반려 |
| 그 단지에 그 평형이 실제로 있는가 | 없으면 반려 |
| 최근 거래가가 실제 실거래와 맞는가 | 다르면 반려 |

**리포트를 읽고 판단하지 않는다. 화면을 직접 확인한다.**

### 6.2 개발 담당 자체 점검

실행 시 아래를 **스크립트가 자동 출력**한다. 서술형 문장으로 쓰지 않는다.

#### 결과 표기 — 세 가지만 쓴다

| 표기 | 뜻 |
| :-- | :-- |
| `정상` | 검사했고 문제 없음 |
| `이상 N건` | 검사했고 문제 발견. **목록을 반드시 붙인다** |
| `확인 못 함` | **필요한 데이터가 없어 검사 자체를 못 했음** |

> **`확인 못 함`을 `정상`이나 `0건`으로 쓰지 않는다.**
> 이전 점검에서 세대수 데이터가 없는 상태로 "세대수 50 미만 단지 0건"이 출력되어, 상위권이 전부 소규모 단지인데도 통과로 넘어갔다.
> 검사 항목에 필요한 데이터가 없으면 **`확인 못 함`과 그 사유**를 적는다.

#### 출력 예시

```
■ 점검 · 2026-07-31

대상 구
  11650 서초구   287
  11680 강남구   324
  그 외             0        ← 0이 아니면 실행 중단

표시 / 제외
  표시   431
  제외   181
    거래 5건 미만       98
    직거래·취소 과다     15
    전고점 없음         68

빈칸
  최근 거래가     0
  전고점          0
  거래량비       12

점검 항목
  1. 그 외 지역 혼입          정상
  2. 빈칸에 추정치 삽입        정상
  3. 화면 내부 용어            정상
  4. 거래 5건 미만 단지 노출   정상
  5. 세대수 대비 거래 과다     확인 못 함 (세대수 자료 없음. 9-8 항목)
  6. 호가 대조                 확인 못 함 (호가 자료 없음. 9-17 항목)

  → 이상 0건 / 확인 못 함 2건

정렬 상위 10건
  1. 반포자이      34평형  서초구 반포동  +5.3%p  11건
  2. 은마          34평형  강남구 대치동  +1.2%p  23건
  ...

거래 5~9건 단지 (숫자 신뢰도 낮음)
  1. ...
```

**"정상"만 쓰고 목록을 안 붙이면 그 점검은 무효다.**

### 6.3 통과 판정

```
이상 0건        →  통과
이상 1건 이상    →  불통과.  개별 항목이 통과여도 전체는 불통과
확인 못 함       →  통과에 영향 없음. 단, 사유와 §9 항목 번호를 적는다
```

**이상이 하나라도 있으면 화면을 갱신하지 않는다.** 이전 점검에서 중앙값 이탈이 `[이상]`으로 찍혔는데 전체는 `통과`로 넘어가고 화면이 갱신됐다. 그런 판정은 무효다.

## 7. 종합 점수는 언제 붙이나

**이번에 점수를 만들지 않는 것은 폐기가 아니라 순서다.**

### 7.1 왜 지금은 정렬만 하나
축 하나하나가 맞는지 확인되지 않은 상태에서 합치면, 결과가 틀렸을 때 **어디가 틀렸는지 찾을 수 없다.**

이전 시도에서 99점이 나왔을 때 원인이 정규화인지, 가중치인지, 부호 처리인지, 평형 매핑인지 구분되지 않았다. 실제 원인은 크롤러였다. 축이 하나면 발주자가 네이버에서 직접 대조해 5분 만에 판정할 수 있다.

### 7.2 붙이는 순서

| 버전 | 축 | 산출 |
| :-- | :-- | :-- |
| v4.0 | 초과하락률, 거래량비 | 정렬 |
| v4.1 | + 전세가율, 연식, 브랜드 | 정렬 |
| v4.2 | + 역세권, 초등학교 | 정렬 |
| **v4.3** | 6개 이상 | **가중합 → 비교군 백분위 → 0~100** |

### 7.3 v4.3에서 붙일 점수의 형태
```
1) 축별로 비교군 내 상대 위치를 구한다 (중위수·MAD 기준)
2) 축을 성격별로 묶고 묶음마다 가중치를 준다
3) 묶음을 합친 뒤 정규분포 누적분포로 0~100에 매핑한다
   → 중앙값 50, 상위 10%가 대략 63점
```
**상한 100으로 자르지 않는다.** 자르는 순간 상위권 내부의 순서가 사라진다.

각 축이 이미 검증을 통과한 상태이므로, 점수가 이상하면 **합치는 로직만 의심하면 된다.**

### 7.4 앞당기지 않는다
축이 6개 미만인 상태에서 점수를 만들자는 요청이 오더라도 거절한다. 가중치를 정할 근거가 없기 때문이다.

## 8. 완료 판정

```
1. 그 외 지역 건수 = 0
2. 빈칸 자리에 추정치가 들어가지 않았음
3. 화면에 내부 용어 0건
4. 상위 10건 목록 + 거래 5~9건 단지 목록 출력
5. 점검 결과가 §6.3 기준으로 통과
6. §9 미구현 목록을 상태와 함께 재출력   ← 필수
```

**5번이나 6번을 빠뜨린 보고는 반려한다.**

---

## 9. 미구현 목록

> **이 목록은 매 보고서 끝에 상태와 함께 다시 출력한다.**
> 항목을 지울 수 있는 경우는 **완료되었을 때**와 **발주자가 취소했을 때** 둘뿐이다.
> 조용히 사라지면 그 보고는 무효다.

### 9.1 데이터·지표

| # | 항목 | 왜 지금 안 하나 | 착수 조건 | 예정 |
| :-- | :-- | :-- | :-- | :-- |
| 1 | 18평 이하·50평 이상 평형 | 강남권 표본이 얇아 비교 기준이 흔들림 | 25/34/44평형이 안정적으로 돌아간 뒤 | v4.2 |
| 2 | 전세가율 | 데이터는 확보됨(전월세 CSV). 지표만 미적용 | v4.0 검증 통과 | **v4.1** |
| 3 | 임대수익률 | 전세가율과 같은 데이터. 우선순위 후순위 | 전세가율 적용 후 | v4.2 |
| 4 | 전고점 시간 감쇠 | 감쇠 계수의 근거가 없음. 지금은 전고점 시점을 표시해 발주자가 판단 | 백테스트로 계수 검증 가능해진 뒤 | v4.4 |
| 5 | 미등기 장기경과 거래 제외 | 등기일자 컬럼의 연도별 결측률 미확인 | 결측률 확인 후 | v4.1 |
| 6 | 매물 수 증감 | 네이버 스냅샷 30일 누적 필요 | 크롤러 실연동 + 30일 | v4.5 |
| 7 | 입주물량·미분양 | 수기 CSV 확보 필요 | 발주자가 자료 확보 | v4.5 |

### 9.2 입지·단지 정보

| # | 항목 | 왜 지금 안 하나 | 착수 조건 | 예정 |
| :-- | :-- | :-- | :-- | :-- |
| 8 | 세대수·연식·브랜드 | 연식·브랜드는 국토부 데이터로 가능. 세대수는 네이버 필요 | v4.0 검증 통과 | **v4.1** (연식·브랜드)<br>v4.3 (세대수) |
| 9 | 역세권 거리 | 카카오 API 연동 필요. 좌표는 도로명 주소로 확보 가능 | v4.1 완료 | v4.2 |
| 10 | 초등학교 거리 | 역세권과 동일 방식 | 역세권 완료 후 | v4.2 |
| 11 | 업무지구 접근시간 | 경로탐색 API 확보 여부 미확인 | API 조사 완료 | v4.4 |
| 12 | 용적률·대지지분 | 데이터 출처 미확정 | 출처 확보 | v4.4 |

### 9.3 점수·판단

| # | 항목 | 왜 지금 안 하나 | 착수 조건 | 예정 |
| :-- | :-- | :-- | :-- | :-- |
| 13 | **종합 점수 (0~100)** | 축이 3개뿐이라 가중치를 정할 근거가 없음. 축별 검증이 먼저 (§7) | 축 6개 이상이 §6.1 검증 통과 | **v4.3** |
| 14 | 비교군 세분화(구·법정동) | 지금은 강남권 전체 기준. 표본이 갖춰지면 세분화 | 지표 안정화 후 | v4.3 |
| 15 | 리스크 감점 | 전세가율·입주물량이 선행되어야 함 | 항목 2, 7 완료 | v4.5 |
| 16 | 판단 문장 (강점/주의) | v3에서 값과 문장이 어긋난 전례. 지표가 확실해진 뒤 | 점수 도입 후 | v4.3 |

### 9.4 매물(호가)

| # | 항목 | 왜 지금 안 하나 | 착수 조건 | 예정 |
| :-- | :-- | :-- | :-- | :-- |
| 17 | 네이버 호가 연동 | **현재 크롤러가 가짜 데이터를 생성하는 상태.** 실연동 필요 | 크롤러 재작성 | v4.3 |
| 18 | 매물 괴리율 | 호가가 있어야 계산 가능 | 항목 17 완료 | v4.3 |
| 19 | 단지 매칭 (네이버↔국토부) | 지금은 국토부 단독이라 매칭 불필요 | 항목 17 완료 | v4.3 |
| 20 | 층·향 보정 | 매물 단위 정보가 있어야 의미 있음 | 항목 18 완료 | v4.5 |

### 9.5 화면·알림

| # | 항목 | 왜 지금 안 하나 | 착수 조건 | 예정 |
| :-- | :-- | :-- | :-- | :-- |
| 21 | **카드형 화면** | **리스트로 먼저 확인. 발주자 요청 시 전환** | 발주자 요청 | 요청 시 |
| 22 | 단지 상세 화면 | 지표가 3개뿐이라 표에 다 들어감 | 지표 6개 이상 | v4.3 |
| 23 | 실거래 이력 차트 | 상세 화면과 함께 | 항목 22와 동시 | v4.3 |
| 24 | 지역 비교 화면 | 표의 필터로 대체 가능 | 지표 안정화 후 | v4.4 |
| 25 | 텔레그램 알림 | 판단 기준이 확정되지 않음 | 점수·판단 문장 완료 | v4.5 |

### 9.6 검증

| # | 항목 | 왜 지금 안 하나 | 착수 조건 | 예정 |
| :-- | :-- | :-- | :-- | :-- |
| 26 | 백테스트 | 지표가 확정되어야 의미 있음 | 지표 6개 이상 | v4.4 |
| 27 | 지표 유효성 측정(IC) | 백테스트 선행 | 항목 26 완료 | v4.4 |
| 28 | 기존 방식과 비교 | 기존 점수 자체가 신뢰 불가 상태 | 새 지표 안정화 후 | v4.4 |

### 9.7 진행 상태 표기법

보고서에 재출력할 때 각 항목에 아래 중 하나를 붙인다.

```
[대기]    아직 시작 안 함
[진행]    작업 중
[완료]    끝남. 다음 보고서부터 목록에서 제외
[지연]    예정 버전을 넘김 → 사유와 새 예정을 반드시 기재
[취소]    발주자가 취소. 사유 기재
```

**[지연]이 두 번 연속 나오면 발주자에게 별도 보고한다.**

---

## 10. 개발 제약

| # | 제약 |
| :-- | :-- |
| **D1** | 대상 구 필터를 `assert`로 강제한다. 위반 시 실행 중단 |
| **D2** | 값이 없으면 빈칸. 추정치·기본값·대체 문구 금지 |
| **D3** | 점검 결과에 **실제 목록**을 붙인다. "0건"만 쓴 보고는 무효 |
| **D4** | 화면 문구에 내부 용어를 쓰지 않는다 |
| **D5** | 설계에 없는 UI 요소를 추가하지 않는다 |
| **D6** | 확인하지 않은 것을 확인했다고 쓰지 않는다. **모르면 "확인 못 했습니다"라고 쓴다** |
| **D10** | **데이터가 없어 검사 못 한 항목을 `0건`이나 `정상`으로 쓰지 않는다.** `확인 못 함`으로 적고 사유와 §9 항목 번호를 붙인다 |
| **D11** | **이상이 1건이라도 있으면 전체 불통과다.** 개별 항목이 통과여도 마찬가지다. 불통과면 화면을 갱신하지 않는다 |
| **D7** | 매 보고서 끝에 §9 미구현 목록을 상태와 함께 재출력한다 |
| **D8** | 발주자에게 SQL·로그·코드를 먼저 내밀지 않는다. 요청받으면 제공한다 |
| **D9** | 계획서를 다시 제출하지 않는다. 막히는 것만 질문한다 |

---

## 11. 이번 조정의 배경 (2026-07-31)

이전 산출 결과에서 상위 10건이 아래와 같이 나왔다.

```
한티(933-0) · 동민맥스빌B동 · 이니그마빌-III · 도곡하이빌 · 원팰리스
비젼(102) · 파크힐 · 한티(933-35) · 대치하나빌 · 에벤에셀빌라트
```

강남 대표 단지가 하나도 없고 전부 소규모 나홀로 단지였다. 점수 중앙값도 46.65로 기준을 벗어났다.

**원인은 같다.** 거래가 3~4건뿐인 단지는 중위값과 전고점이 한두 건으로 결정되어 하락률이 극단적으로 튀고, 그 극단값이 분포 전체를 끌어당긴다. 최소 거래 건수 3건이 너무 낮았다.

| 조정 | 이전 | 이후 |
| :-- | :--: | :--: |
| 12개월 최소 거래 | 3건 | **5건** |
| 전고점 창 최소 거래 | 2건 | **3건** |
| 거래 5~9건 | 구분 없음 | **`거래 적음` 표시** |

그리고 점검 항목 8개 중 5개가 **데이터가 없어 검사 자체를 못 한 상태로 "0건 정상"** 이 찍혔다. 세대수 자료가 없는데 "세대수 50 미만 단지 0건"이 통과로 나온 것이 대표적이다. §6.2의 `확인 못 함` 표기가 이 문제의 대응이다.

## 12. 다음 버전에서 할 것

**v4.1** — 전세가율 · 연식 · 브랜드 · 미등기 거래 제외 (§9 항목 2, 5, 8)
착수 조건: v4.0이 §6.1 발주자 검증을 통과할 것
